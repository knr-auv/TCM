cmake_minimum_required(VERSION 3.20)
project("TMC" C ASM)
add_compile_definitions(MODULE_SETTINGS="")
add_compile_definitions(DEVICE_TYPE=1 SERIAL_NUMBER=1)
include_directories(CMSIS/Include)
add_subdirectory(TMC)